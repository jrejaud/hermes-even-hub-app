node_modules/
dist/
*.ehpk
.DS_Store
*.log
.env.local
.env.development.local

# local simulator debug captures
docs/e2e/*.png

# bridge runtime config — contains host URLs and references to token env vars
bridge/hosts.json
bridge/.env

# playwright-cli session artifacts (screenshots, snapshots)
.playwright-cli/

# beta build credentials (baked into a testing-group .ehpk on purpose)
.env.beta.local

# UI gallery captures — regenerate with `npm run ui:capture`
docs/ui/*.png

bridge/.env.tab-agent
